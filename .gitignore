bin/
dist/
*.exe
*.tar.gz
*.zip

# The binary `go build -o bomly-plugin-meme-auditor ./cmd/...` produces at the repo root.
# bin/ and dist/ were ignored; a bare root-level binary was not, and ~19MB of
# build output reached main through the gap.
/bomly-plugin-meme-auditor